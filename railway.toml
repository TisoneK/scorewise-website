[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway volume mount for persistent SQLite
# Create a volume named "scorewise-data" in Railway dashboard,
# then mount it at /data
# [[deploy.volumes]]
# name = "scorewise-data"
# mountPath = "/data"
